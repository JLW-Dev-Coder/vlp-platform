import HelpClient from './HelpClient';

export const metadata = {
  title: 'Help Center | Tax Monitor Pro',
  description: 'Get help and support for Tax Monitor Pro.',
};

export default function HelpPage() {
  return <HelpClient />;
}
