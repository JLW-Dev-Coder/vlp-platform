'use client'

// Phase 2 will replace this with a line-by-line port of the SD-built landing page,
// preserving the .tpp-lp CSS namespace and animations. See RC prompt §11 + Deviations 5/6/9.
//
// TODO(RC): Port SD HTML/CSS into this component when source is provided.
export default function LandingPage() {
  return null
}
